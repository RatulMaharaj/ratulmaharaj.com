---
import type { PaginationLink } from "@/types";

export interface Props {
	prevUrl?: PaginationLink;
	nextUrl?: PaginationLink;
}

const { prevUrl, nextUrl } = Astro.props as Props;
---

{
	(prevUrl || nextUrl) && (
		<nav class="mt-8 flex items-center gap-x-6 border-t border-border pt-6">
			{prevUrl && (
				<a
					class="line-clamp-1 py-2 text-sm text-surface-content/70 transition-colors duration-200 hover:text-primary"
					href={prevUrl.url}
				>
					{prevUrl.srLabel && <span class="sr-only">{prevUrl.srLabel}</span>}
					{prevUrl.text ? prevUrl.text : "Previous"}
				</a>
			)}
			{nextUrl && (
				<a
					class="ml-auto line-clamp-1 py-2 text-right text-sm text-surface-content/70 transition-colors duration-200 hover:text-primary"
					href={nextUrl.url}
				>
					{nextUrl.srLabel && <span class="sr-only">{nextUrl.srLabel}</span>}
					{nextUrl.text ? nextUrl.text : "Next"}
				</a>
			)}
		</nav>
	)
}
