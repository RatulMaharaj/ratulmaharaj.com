---
import type { PaginationLink } from "@/types";

export interface Props {
	prevUrl?: PaginationLink;
	nextUrl?: PaginationLink;
}

const { prevUrl, nextUrl } = Astro.props as Props;
---

{
	(prevUrl || nextUrl) && (
		<nav class="flex items-center gap-x-4">
			{prevUrl && (
				<a
					class="cursor-pointer rounded-md py-2 transition-all duration-500 hover:bg-surface-content/5 hover:px-4"
					href={prevUrl.url}
				>
					{prevUrl.srLabel && <span class="sr-only">{prevUrl.srLabel}</span>}
					{prevUrl.text ? prevUrl.text : "Previous"}
				</a>
			)}
			{nextUrl && (
				<a
					class="ml-auto cursor-pointer rounded-md py-2 transition-all duration-500 hover:bg-surface-content/5 hover:px-4"
					href={nextUrl.url}
				>
					{nextUrl.srLabel && <span class="sr-only">{nextUrl.srLabel}</span>}
					{nextUrl.text ? nextUrl.text : "Next"}
				</a>
			)}
		</nav>
	)
}
