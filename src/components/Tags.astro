---
import { tagUrl } from "@/util";
export interface Props {
	allTags: string[];
}
const { allTags } = Astro.props as Props;
---

<aside>
	<h2 class="section-label pb-3">Tags</h2>
	<ul class="flex flex-wrap gap-x-3 gap-y-1.5 sm:flex-col sm:gap-y-1">
		{
			allTags.map((tag) => (
				<li>
					<a
						class="tag"
						href={tagUrl(tag)}
						aria-label={`View all posts with the tag: ${tag}`}
					>
						#{tag}
					</a>
				</li>
			))
		}
	</ul>
	<a
		class="mt-4 block text-xs text-surface-content/55 transition-colors duration-200 hover:text-primary"
		href="/tags"
		aria-label="View all blog categories"
	>
		view all &rarr;
	</a>
</aside>
